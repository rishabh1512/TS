## Local TypeScript Setup
Note :: Prerequest install node & npm

Steps
- Run npm i -g typescript
- Run npm init 
- Add script in package.json 
  <code>{
    tsc : tsc
  }</code>
- Run tsc init
- Change outDir property in tsconfig.json and include property in tsonfig.json


 
